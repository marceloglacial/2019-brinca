import HeadComponent from '../components/Head/Head';
import Header from '../components/Header/Header';

const Home = props => {
    return (
        <>
            <HeadComponent />
            <Header />
        </>
    );
};

export default Home;
